import { render, screen } from "@testing-library/react"
import React from "react"

describe("Player", () => {
  it("manually resets a given player choice", () => {})
  it("manually resets all player choices", () => {})
  it("automatically resets all player choices when turn ends", () => {})
  it("maintains all player choices for the given turn", () => {})
})
